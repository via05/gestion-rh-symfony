<form method="post" action="{{ path('app_gr_user_delete', {'id': gr_user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gr_user.id) }}">
    <button class="btn">Delete</button>
</form>
