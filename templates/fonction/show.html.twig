{% extends 'base.html.twig' %}

{% block title %}Fonction{% endblock %}

{% block body %}
    <h1>Fonction</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fonction.id }}</td>
            </tr>
            <tr>
                <th>FonctionName</th>
                <td>{{ fonction.fonctionName }}</td>
            </tr>
            <tr>
                <th>FonctionDescription</th>
                <td>{{ fonction.fonctionDescription }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_fonction_index') }}">back to list</a>

    <a href="{{ path('app_fonction_edit', {'id': fonction.id}) }}">edit</a>

    {{ include('fonction/_delete_form.html.twig') }}
{% endblock %}
